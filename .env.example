# Port the Express server listens on
PORT=3000

# Base URL of your local Ollama server (started via `ollama serve` or the Ollama desktop app)
OLLAMA_HOST=http://localhost:11434

# Ollama model to use for reviews. Pull it first with: ollama pull llama3.1:8b
OLLAMA_MODEL=llama3.1:8b
